# Created automatically after running: npx convex dev
CONVEX_DEPLOYMENT=dev:your-deployment-name
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# Step 17 auth setup (set these in the Convex dashboard / `npx convex env set`,
# NOT as VITE_ variables and never commit real secrets here):
# SITE_URL=https://your-production-site.example
# AUTH_GOOGLE_ID=your-google-oauth-client-id
# AUTH_GOOGLE_SECRET=your-google-oauth-client-secret
# RESEND_API_KEY=re_xxxxxxxxx
# AUTH_EMAIL_FROM=Ceventic <auth@your-verified-domain.com>
